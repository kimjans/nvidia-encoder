{
  "name": "nvidia-encoder",
  "version": "1.0.0",
  "description": "Encoder by Nvidia-sdk and EGL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "node-addon-api": "^3.1.0"
  }
}
